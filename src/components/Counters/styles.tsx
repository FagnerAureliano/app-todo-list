import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    gap: 10,
    alignItems: "center",
  },

  counters: {
    fontWeight: "700",
    fontSize: 12,
    color: "#FFF",
    paddingHorizontal: 8,
    paddingVertical: 2,
    backgroundColor: "#333333",
    borderRadius: 10,
  },
});
